version: "3.9"

services:

  api:
    build: .
    container_name: ServerFastAPI_SGAFA
    ports:
      - "8080:5000"
    volumes:
      - .:/app
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      DATABASE_URL: postgresql://admin:123456@postgres:5432/DB_miapiSGAFA

  postgres:
    image: postgres:15
    container_name: ServerPostgreSql_SGAFA
    restart: always
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: 123456
      POSTGRES_DB: DB_miapiSGAFA
    ports:
      - "5436:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U admin -d DB_miapiSGAFA"]
      interval: 5s
      retries: 5
      timeout: 5s

volumes:
  postgres_data:
